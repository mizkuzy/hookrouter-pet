import React from "react";
import Page from "../components/Page";

const NotFound = () => {
    return (
        <div>
            <Page title={"NotFound"}>Not Found page</Page>
        </div>
    );
};

export default NotFound;
